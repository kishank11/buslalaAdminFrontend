import React from 'react'

const Edit = ({ title }) => {
    return (
        <button className='bg-cyan-800 h-7 w-20 rounded-md text-white'>
            {title}
        </button>
    )
}

export default Edit
